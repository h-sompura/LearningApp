<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/appHeading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="1dp"
        android:layout_marginEnd="78dp"
        android:fontFamily="@font/source_sans_pro_semibold"
        android:text="Learn HTML and CSS 💻"
        android:textSize="24dp" />

    <androidx.appcompat.widget.SwitchCompat
        android:id="@+id/switch_sequential_progress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/appHeading"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="36dp"
        android:layout_marginEnd="103dp"
        android:text="Force sequential progress?" />

    <ListView
        android:id="@+id/listview_lesson_list"
        android:layout_width="match_parent"
        android:layout_height="500dp"
        android:layout_above="@+id/logoutButton"
        android:layout_below="@id/switch_sequential_progress"
        android:layout_marginTop="27dp"
        android:layout_marginBottom="45dp">

    </ListView>

    <Button
        android:id="@+id/logoutButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginEnd="99dp"
        android:layout_marginBottom="25dp"
        android:text="Logout" />

</RelativeLayout>